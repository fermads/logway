{
  "name": "ferlog",
  "version": "0.0.1",
  "description": "Client and server application for sending metrics and logs from the client (browser) to the server (Graphite / Logstash)",
  "main": "index.js",
  "bin": "./index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "graphite-mock": "node4 --use_strict server/test/graphite-mock.js",
    "start4": "node4 --use_strict .",
    "start": "node --use_strict .",
    "build": "uglifyjs client/src/ferlog.js client/src/ferlog.perf.js client/src/ferlog.weblog.js client/src/ferlog.timer.js -o client/build/ferlog.min.js"
  },
  "keywords": [
    "graphite",
    "logstash",
    "client",
    "server"
  ]
}
